
    experiment_name: test_baseline_lr
    model:
      type: baseline
      params:
        name: LogisticRegression
        solver: liblinear
    data:
      dataset_name: abide
      batch_size: 32
      num_workers: 4
    training:
      max_epochs: 10
      learning_rate: 0.001
    