data:
  batch_size: 32
  dataset_name: abide
  num_workers: 4
experiment_name: test_baseline_lr
model:
  params:
    name: LogisticRegression
    solver: liblinear
  type: baseline
training:
  learning_rate: 0.001
  max_epochs: 10
